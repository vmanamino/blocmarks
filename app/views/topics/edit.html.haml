%h1 Edit Topic
.row
  .col-md-4
    .col-md-8
      = form_for @topic do |f|
        .form-group
          = f.label :title
          = f.text_field :title, class: 'form-control', placeholder: 'Enter topic'
        .form-group
          = f.submit "Save", class: 'btn btn-success'